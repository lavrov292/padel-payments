# Интеграция с API Точка банк

## Текущий статус

✅ **Скрипты созданы и готовы к использованию**
- `test_payment.py` - создание платёжных ссылок (простой токен)
- `test_payment_oauth.py` - создание платёжных ссылок с OAuth 2.0 (из Postman окружения)
- `test_other_methods.py` - тестирование других методов API

⚠️ **Требуется регистрация в личном кабинете Точка банк для полноценной работы**

## Результаты тестирования песочницы

### ✅ Работает:
- API доступен: `/health` возвращает `200 OK`
- Гибридный токен: `sandbox.jwt.token` принимается API

### ✅ Работает в песочнице:
- **QR-коды СБП**: `/sbp/v1.0/qr-code/merchant/{merchantId}/{accountId}` - **РАБОТАЕТ!** ✅

### ❌ Не работает в песочнице (требуется регистрация):
- Платёжные ссылки: `/acquiring/payment-links` возвращает `501 Not Implemented`
- Счета: `/accounts` возвращает `404 Not Found`
- Балансы: `/balances` возвращает `404 Not Found`
- OAuth 2.0 токены: `/oauth2/token` возвращает `404 Not Found`
- Список QR-кодов: GET запрос возвращает `501 Not Implemented`

## Настройки из Postman окружения

Используются данные из [Sandbox.postman_environment.json](https://developers.tochka.com/assets/files/Sandbox.postman_environment-e02d431aa01391fe2f9a22e31b5306d6.json):

```python
AS_URL = "https://enter.tochka.com"  # Authorization Server
RS_URL = "https://enter.tochka.com/sandbox/v2"  # Resource Server
CLIENT_ID = "test_app"
CLIENT_SECRET = "test_secret"
SCOPE = "accounts balances customers statements sbp payments acquiring"
TEST_ACCOUNT_ID = "12345810901234567890/044525104"  # Тестовый счёт
HYBRID_TOKEN = "sandbox.jwt.token"  # Гибридный токен для песочницы
```

## Что нужно сделать для полноценной работы

### 1. Регистрация в личном кабинете Точка банк

1. Зарегистрируйтесь на [https://enter.tochka.com](https://enter.tochka.com)
2. Получите доступ к API в настройках аккаунта
3. Создайте приложение и получите реальные токены (не `sandbox.jwt.token`)

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта или в папке `sandbox/tochka/`:

```env
# Точка банк API
TOCHKA_API_BASE_URL=https://enter.tochka.com/uapi/v2
TOCHKA_API_TOKEN=ваш_реальный_токен
TOCHKA_SANDBOX_TOKEN=sandbox.jwt.token  # Для песочницы
TOCHKA_SANDBOX_URL=https://enter.tochka.com/sandbox/v2
```

### 3. Использование скриптов

#### Тестовый платёж в песочнице:
```bash
cd sandbox/tochka
python3 test_payment.py
```

#### Тестирование других методов:
```bash
python3 test_other_methods.py
```

## Документация API

- [Официальная документация](https://developers.tochka.com/docs/tochka-api/)
- [Песочница](https://developers.tochka.com/docs/tochka-api/pesochnica)
- [Платёжные ссылки](https://developers.tochka.com/docs/tochka-api/opisanie-metodov/platyozhnye-ssylki)
- [СБП (Система быстрых платежей)](https://developers.tochka.com/docs/tochka-api/servis-sbp-rabota-s-qr-kodami)

## Возможности API Точка банк

- 💰 Работа с балансами счетов
- 📄 Получение выписок
- 🔗 Создание платёжных ссылок
- 📱 СБП: QR-коды для оплаты
- 🔔 Вебхуки о входящих/исходящих платежах
- 📋 Выставление счетов
- 💳 Работа с платежами

## Следующие шаги

1. Зарегистрируйтесь в личном кабинете Точка банк
2. Получите реальные токены доступа
3. Обновите скрипты для использования реальных токенов
4. Протестируйте создание платёжных ссылок
5. Интегрируйте в основной проект (замена или дополнение YooKassa)

## Примечания

- Песочница может иметь ограниченный функционал
- Для полноценной работы требуется регистрация и настройка в личном кабинете
- Токен `sandbox.jwt.token` работает только для базовых проверок подключения
- Данные из Postman окружения (`postman_config.json`) используются в `test_payment_oauth.py`
- Тестовый `accountId` из Postman: `12345810901234567890/044525104` можно использовать для тестирования после регистрации

## Файлы проекта

- `test_payment.py` - простой тест с гибридным токеном
- `test_payment_oauth.py` - тест с OAuth 2.0 и данными из Postman
- `test_other_methods.py` - тестирование различных методов API
- `test_sbp_qr.py` - **регистрация QR-кода СБП** (работает в песочнице!) ✅
- `check_payment_status.py` - **проверка статуса оплаты QR-кода** ✅
- `postman_config.json` - настройки из Postman окружения для песочницы
- `requirements.txt` - зависимости Python
- `last_qr_code.json` - автоматически создаётся при создании QR-кода (для удобства)

## Регистрация QR-кода СБП ✅ РАБОТАЕТ!

Скрипт `test_sbp_qr.py` создан для регистрации QR-кодов СБП в песочнице и **успешно протестирован!**

### Использование:

```bash
# С merchant_id и account_id из аргументов
python3 test_sbp_qr.py <merchant_id> [account_id] [terminal_id]

# Или используйте тестовые данные по умолчанию
python3 test_sbp_qr.py
```

### Пример с тестовыми данными:

```bash
# Использует тестовые данные из скрипта
python3 test_sbp_qr.py

# Или с явным указанием
python3 test_sbp_qr.py "200000000001097" "12345123451234512345/044525104"
```

### Тестовые данные из песочницы:

**Accounts:**
- `12345123451234512345/044525104`
- `12345678901234567890/044525104`
- `12345810901234567890/044525104`

**Merchants:**
- `200000000001097` (Terminal: `20000097`, Name: OOO ALTERO)
- `200000000001098` (Terminal: `20000090`, Name: OOO LUCH)

### Формат запроса:

- **Эндпоинт:** `POST /sbp/v1.0/qr-code/merchant/{merchantId}/{accountId}`
- **Токен:** `sandbox.jwt.token`
- **Payload:**
  ```json
  {
    "data": {
      "payment_purpose": "Назначение платежа",
      "qrc_type": "01",  // "01" для статического, "02" для динамического
      "amount": 10000    // только для динамического (в копейках)
    }
  }
  ```

### Результат:

При успешном создании QR-кода вы получите:
- `qrcId` - ID QR-кода
- `payload` - URL для оплаты через СБП
- `image` - изображение QR-кода в base64 (PNG, 300x300)

Информация о созданном QR-коде сохраняется в `last_qr_code.json` для удобства.

## ⚠️ ВАЖНО: Ограничения песочницы

**В песочнице Точка Банка НЕВОЗМОЖНО провести реальную оплату через СБП!**

Песочница предназначена только для:
- ✅ Тестирования API запросов (создание QR-кодов)
- ✅ Проверки формата ответов
- ✅ Интеграции с API

Песочница НЕ поддерживает:
- ❌ Реальные платежи через СБП
- ❌ Проверку статуса реальных платежей
- ❌ Оплату через мобильное приложение банка

**Для тестирования реальных платежей:**
- Используйте боевой контур с минимальными суммами
- Обратитесь в Точка Банк для получения доступа к тестовому боевому контуру

## Создание QR-кода в песочнице

### 1. Создание тестового QR-кода

```bash
python3 test_sbp_qr.py
```

После создания вы получите:
- QR-код в виде изображения (сохраняется в файл)
- URL для оплаты: `https://qr.nspk.ru/...` (не работает в песочнице!)
- `qrcId` для проверки статуса (только для боевого контура)

**Примечание:** QR-коды, созданные в песочнице, недействительны для реальной оплаты. Они созданы только для проверки формата API ответов.

### 2. Что можно протестировать в песочнице:

1. **Создание QR-кода** - проверка формата запроса и ответа
2. **Получение изображения** - проверка, что изображение корректно формируется
3. **Формат данных** - проверка структуры ответа API

### 3. Проверка статуса оплаты (только для боевого контура)

**⚠️ В песочнице проверка статуса не работает, так как реальные платежи невозможны.**

Для боевого контура (после регистрации):

**Быстрая проверка:**
```bash
python3 check_payment_status.py --last
```

**С явным указанием параметров:**
```bash
python3 check_payment_status.py <qrc_id> <merchant_id> <account_id>
```

**Отслеживание статуса в реальном времени:**
```bash
python3 check_payment_status.py --watch <qrc_id> [merchant_id] [account_id]
```

### Что возвращает API при проверке статуса (в боевом контуре):

- `status` / `paymentStatus` - статус оплаты (например, "paid", "completed", "pending")
- `amount` - сумма платежа (в копейках)
- `paymentDate` / `date` - дата и время оплаты
- `transactionId` - ID транзакции

## Следующие шаги для реального тестирования:

1. **Зарегистрируйтесь в личном кабинете Точка Банк**
2. **Получите доступ к боевому контуру** (или тестовому боевому контуру)
3. **Используйте реальные токены** вместо `sandbox.jwt.token`
4. **Измените URL** с `/sandbox/v2` на `/uapi/v2` (или другой боевой URL)
5. **Создайте QR-код в боевом контуре** - тогда его можно будет оплатить реально
6. **Проверьте статус** после реальной оплаты

## Интеграция в проект (аналог YooKassa)

**✅ API Точка Банк полностью поддерживает создание уникальных платежей и получение вебхуков!**

См. подробное сравнение с YooKassa в файле: **[TOCHKA_VS_YOOKASSA.md](./TOCHKA_VS_YOOKASSA.md)**

**Ключевые возможности:**
- ✅ Уникальные идентификаторы платежей (`paymentLinkId`)
- ✅ Вебхуки для уведомлений об оплате (`acquiringInternetPayment`)
- ✅ Платёжные ссылки для СБП и карт
- ✅ QR-коды СБП с отслеживанием статуса
- ✅ Проверка статуса платежа через API

**Можно реализовать аналогично YooKassa:**
- Создание уникального платежа для каждого `entry_id`
- Сохранение связи `entry_id` ↔ `payment_link_id` в БД
- Обработка вебхуков для автоматического обновления статуса
- Проверка статуса платежа через API

