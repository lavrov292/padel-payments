# Проверка автозапуска импорта и обработки pending entries

## ✅ Автоматический вызов обработки pending entries

**УЖЕ РЕАЛИЗОВАНО** в коде `scripts/import_lunda.py` (строки 1450-1472).

После каждого импорта автоматически вызывается:
- `/admin/process-new-entries` - для отправки уведомлений о новых записях
- `/admin/process-pending-players` - для отправки уведомлений о спорных именах игроков

Это происходит в функции `main()` после завершения импорта, **не нужно вызывать вручную**.

### Как проверить в логах:

После импорта должны быть строки:
```
AUTO-TRIGGERING PENDING PLAYERS PROCESSING
AUTO PENDING: status=200, found=X, notified=Y
```

---

## 🔍 Проверка автозапуска скрипта импорта

Автозапуск скрипта `scripts/import_lunda.py` зависит от платформы:

### Вариант 1: Render.com (вероятно ваш случай)

1. Зайди в Render Dashboard: https://dashboard.render.com
2. Выбери свой сервис (padel-payments)
3. Перейди в раздел **"Cron Jobs"** или **"Scheduled Jobs"**
4. Проверь, есть ли там задача с командой:
   ```
   python scripts/import_lunda.py
   ```
   или
   ```
   python3 scripts/import_lunda.py
   ```

5. Проверь расписание (например: `0 */6 * * *` = каждые 6 часов)

**Если Cron Job НЕ настроен:**
- Создай новый Cron Job в Render Dashboard
- Команда: `python scripts/import_lunda.py` (или `python3`)
- Расписание: например `0 */6 * * *` (каждые 6 часов)
- Убедись, что переменные окружения (DATABASE_URL, BACKEND_BASE_URL и т.д.) доступны

---

### Вариант 2: macOS (launchd)

Если скрипт запускается на Mac через launchd:

```bash
# Проверить все launchd задачи
launchctl list | grep import

# Или посмотреть plist файлы
ls ~/Library/LaunchAgents/ | grep import
ls /Library/LaunchDaemons/ | grep import
```

---

### Вариант 3: Linux (systemd/cron)

**Cron:**
```bash
# Проверить crontab текущего пользователя
crontab -l

# Проверить системный crontab
cat /etc/crontab
ls /etc/cron.d/
```

**Systemd:**
```bash
# Проверить systemd таймеры
systemctl list-timers | grep import

# Проверить systemd сервисы
systemctl list-units | grep import
```

---

## 🧪 Тестирование автоматического вызова

Чтобы проверить, что автоматический вызов работает:

1. **Запусти импорт вручную:**
   ```bash
   python scripts/import_lunda.py
   ```

2. **Проверь логи** - в конце должны быть строки:
   ```
   AUTO-TRIGGERING PENDING PLAYERS PROCESSING
   ==================================================
   AUTO PENDING: status=200, found=X, notified=Y
   ```

3. **Проверь Telegram** - админ должен получить уведомления о pending entries (если они есть)

---

## 📝 Резюме

✅ **Автоматический вызов `/admin/process-pending-players`** - УЖЕ РЕАЛИЗОВАН  
   → Вызывается автоматически после каждого импорта  
   → Не нужно вызывать вручную

❓ **Автозапуск скрипта импорта** - нужно проверить в Render Dashboard  
   → Зайди в раздел "Cron Jobs"  
   → Если нет - создай новый Cron Job с расписанием



